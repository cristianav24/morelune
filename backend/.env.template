# Base de datos
DATABASE_URL=postgres://bags_user:bags_password@postgres:5432/bags_db

# Redis
REDIS_URL=redis://redis:6379

# URLs (cambiar por tu dominio en producción)
MEDUSA_BACKEND_URL=https://api.tudominio.com
STORE_CORS=https://tudominio.com,http://localhost:3000
ADMIN_CORS=https://admin.tudominio.com,http://localhost:9000
AUTH_CORS=https://tudominio.com,https://admin.tudominio.com,http://localhost:3000

# Seguridad
JWT_SECRET=cambia_esto_por_un_secreto_muy_largo_y_aleatorio
COOKIE_SECRET=cambia_esto_por_otro_secreto_muy_largo_y_aleatorio

# Culqi (pasarela de pagos Perú)
# Culqi usa su propio SDK — ver src/modules/culqi/
CULQI_PUBLIC_KEY=pk_live_xxxxxxxxxxxxxxxxxx
CULQI_SECRET_KEY=sk_live_xxxxxxxxxxxxxxxxxx
CULQI_WEBHOOK_SECRET=wh_xxxxxxxxxxxxxxxxxx

# Email (opcional)
SENDGRID_API_KEY=
SENDGRID_FROM=noreply@tudominio.com
SENDGRID_ORDER_PLACED_TEMPLATE=
SENDGRID_SHIPMENT_TEMPLATE=

# Admin
MEDUSA_ADMIN_EMAIL=admin@tudominio.com
MEDUSA_ADMIN_PASSWORD=CambiarEstaContraseña123!
